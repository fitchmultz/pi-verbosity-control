name: npm publication held

# This Git fork is delivered from fitchmultz/pi-verbosity-control.
# The inherited pi-verbosity-control npm identity belongs to ferologics; publisher
# rights and artifact identity have NOT been verified. Do not publish on releases.
# Re-enabling publication requires verified ownership, same-SHA compatibility,
# tag/package identity checks, and the owner's release review approvals.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    # Permanently disabled: always() is true, so this condition is always false.
    if: ${{ !always() }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Publication held; this Git fork has no verified npm publishing channel.'
